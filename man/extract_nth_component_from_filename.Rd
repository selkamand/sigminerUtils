% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SigminerToSigstory.R
\name{extract_nth_component_from_filename}
\alias{extract_nth_component_from_filename}
\title{Extract the N-th component from filenames}
\usage{
extract_nth_component_from_filename(filenames, n = 1, sep = ".", fixed = TRUE)
}
\arguments{
\item{filenames}{A character vector of filenames.}

\item{n}{An integer specifying which component to extract (default is 1).
Negative values of \code{n} extract components from the end of the filename.}

\item{sep}{A character string used to split the filenames (default is ".").}

\item{fixed}{Logical, whether to treat \code{sep} as a literal string (default is TRUE).}
}
\value{
A character vector containing the N-th component from each filename,
or \code{NA} if unavailable.
}
\description{
Splits each filename by a specified separator and returns the N-th component.
If the N-th component doesn't exist, returns \code{NA}.
Supports negative values for \code{n}, where \code{n = -1} returns the last component,
\code{n = -2} returns the second-to-last, and so on.
}
\examples{
# Example: Extracting the second component from filenames
extract_nth_component_from_filename(c("file.name.extension", "another.file.txt"), n = 2)
# Returns: c("name", "file")

# Example: Extracting the last component from filenames
extract_nth_component_from_filename(c("file.name.extension", "another.file.txt"), n = -1)
# Returns: c("extension", "txt")

}
\keyword{internal}
